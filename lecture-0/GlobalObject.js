/*
    All variables and functions are actually parameters and methods
    on the global object.

    Browser global object is the 'window' object
    Node.js global object is the 'global' object
*/

console.log(global)

/*
    On a browser:
    
    window 

    var x = 50

    window.x
*/